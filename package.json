{
  "name": "app-functions",
  "version": "1.0.0",
  "private": "true",
  "dependencies": {
    "jwt-decode": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "scripts": {
    "build": "tsc -p ./tsconfig.json && echo 'builded the betty functions'",
    "publish": "npm run build && bb functions publish"
  }
}
