{
  "name": "app-functions",
  "version": "1.0.0",
  "private": "true",
  "devDependencies": {
    "@betty-blocks/cli": "^25.98.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "format": "prettier ./src --write",
    "build": "npm run format && tsc -p ./tsconfig.json && echo 'builded the betty functions'",
    "publish": "npm run build && bb functions publish"
  }
}
